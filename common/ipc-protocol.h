
typedef enum
{
    IPC_STATUS_OK      = 0x00,
    IPC_STATUS_ERROR   = 0x01,
    IPC_STATUS_INVALID = 0x02,

} ipc_status_t;

typedef enum
{
    IPC_STATUS_OK      = 0x00,
    IPC_STATUS_ERROR   = 0x01,
    IPC_STATUS_INVALID = 0x02,

} ipc_status_t;